{
	"name": "face_game",
	"version": "1.0.0",
	"description": "DDR-esque game with face-api",
	"main": "index.js",
	"dependencies": {
		"typescript": "^4.0.5"
	},
	"devDependencies": {
		"face-api.js": "^0.22.2"
	},
	"scripts": {
		"build": "tsc && cp -R src/public/* dist",
		"prestart": "npm run build",
		"start": "node .",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"keywords": [],
	"author": "",
	"license": "MIT"
}
